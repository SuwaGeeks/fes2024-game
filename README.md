# ぴゅんぴゅん2

## 環境構築
追加のパッケージを使用するので `vnev` というpythonの仮想環境の仕様をおすすめします．  
仮想環境の作成及びアクティベート
```bash
python3 -m venv .venv
. ./.venv/bin/activate
```
パッケージのインストール
```bash
pip install -r requirements.txt
```